'use client'

import { createClient } from '@/lib/supabase/client'
import { useEffect, useState } from 'react'

const empty = { business_name:'', description:'', address:'', phone:'', website:'', hours:'', holidays:'', pricing:'', faqs:'', tone:'Warm, professional, and concise' }
export default function Dashboard() {
  const [profile, setProfile] = useState(empty); const [email, setEmail] = useState(''); const [testEmail, setTestEmail] = useState(''); const [reply, setReply] = useState(''); const [saving, setSaving] = useState(false); const [loading, setLoading] = useState(false); const [message, setMessage] = useState('')
  useEffect(() => { (async () => { const s = createClient(); const { data } = await s.auth.getUser(); if (!data.user) return; setEmail(data.user.email || ''); const { data: p } = await s.from('business_profiles').select('business_name,description,address,phone,website,hours,holidays,pricing,faqs,tone').eq('user_id', data.user.id).maybeSingle(); if (p) setProfile(p) })() }, [])
  function update(key: keyof typeof empty, value: string) { setProfile((p) => ({ ...p, [key]: value })) }
  async function save() { setSaving(true); const s = createClient(); const { data: u } = await s.auth.getUser(); if (u.user) await s.from('business_profiles').upsert({ ...profile, user_id: u.user.id, updated_at: new Date().toISOString() }); setMessage('Business profile saved.'); setSaving(false) }
  async function generate() { setLoading(true); setReply(''); const r = await fetch('/api/generate-reply', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({ email:testEmail, business:profile }) }); const data = await r.json(); setReply(data.reply || data.error || 'No reply generated.'); setLoading(false) }
  async function logout() { await createClient().auth.signOut(); location.href = '/' }
  return <main className="min-h-screen bg-background"><header className="border-b bg-card"><div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5"><a href="/" className="font-semibold text-primary">MailMind AI</a><div className="flex items-center gap-4"><span className="hidden text-sm text-muted-foreground sm:block">{email}</span><button onClick={logout} className="text-sm font-medium text-muted-foreground hover:text-foreground">Sign out</button></div></div></header><div className="mx-auto max-w-6xl px-6 py-12"><div className="mb-10"><p className="text-sm font-medium text-primary">Business workspace</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Train your email assistant</h1><p className="mt-3 max-w-2xl leading-6 text-muted-foreground">Your profile is private to this Google account. MailMind uses it to write replies that sound like your business.</p></div><div className="grid gap-8 lg:grid-cols-[1.1fr_.9fr]"><section className="rounded-2xl border bg-card p-6 shadow-sm"><div className="mb-6 flex items-center justify-between"><h2 className="text-xl font-semibold">Business profile</h2><button onClick={save} disabled={saving} className="rounded-lg bg-primary px-4 py-2 text-sm font-medium text-primary-foreground disabled:opacity-60">{saving?'Saving…':'Save profile'}</button></div><div className="grid gap-4 sm:grid-cols-2">{[['business_name','Business name'],['address','Location / address'],['phone','Phone'],['website','Website'],['hours','Opening hours'],['holidays','Holidays'],['pricing','Pricing'],['tone','Reply tone']].map(([key,label])=><label key={key} className="grid gap-2 text-sm font-medium">{label}<input value={profile[key as keyof typeof empty]} onChange={e=>update(key as keyof typeof empty,e.target.value)} className="rounded-lg border bg-background px-3 py-2 font-normal outline-none ring-primary focus:ring-2" /></label>)}<label className="grid gap-2 text-sm font-medium sm:col-span-2">What does your business do?<textarea value={profile.description} onChange={e=>update('description',e.target.value)} rows={3} className="rounded-lg border bg-background px-3 py-2 font-normal outline-none ring-primary focus:ring-2" /></label><label className="grid gap-2 text-sm font-medium sm:col-span-2">FAQs and policies<textarea value={profile.faqs} onChange={e=>update('faqs',e.target.value)} rows={4} placeholder="Paste common questions, refund policy, booking process…" className="rounded-lg border bg-background px-3 py-2 font-normal outline-none ring-primary focus:ring-2" /></label></div>{message && <p className="mt-4 text-sm text-primary">{message}</p>}</section><section className="rounded-2xl border bg-card p-6 shadow-sm"><p className="text-sm font-medium text-primary">Free test</p><h2 className="mt-2 text-xl font-semibold">Generate a sample reply</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Try the assistant before connecting Gmail. Your business context is included automatically.</p><textarea value={testEmail} onChange={e=>setTestEmail(e.target.value)} rows={7} placeholder="Paste a customer email here…" className="mt-6 w-full rounded-lg border bg-background px-3 py-2 text-sm outline-none ring-primary focus:ring-2" /><button onClick={generate} disabled={loading || !testEmail.trim()} className="mt-3 w-full rounded-lg bg-primary px-4 py-3 text-sm font-medium text-primary-foreground disabled:opacity-50">{loading?'Writing reply…':'Generate AI reply'}</button>{reply && <div className="mt-5 rounded-lg bg-secondary p-4 text-sm leading-6 whitespace-pre-wrap">{reply}</div>}<div className="mt-8 border-t pt-6"><h3 className="font-semibold">Gmail connection</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Connect Gmail after your profile is ready. Google will ask you to approve the read and reply permissions.</p><button disabled className="mt-4 w-full rounded-lg border px-4 py-3 text-sm font-medium opacity-60">Connect Gmail — setup required</button></div></section></div></div></main>
}
